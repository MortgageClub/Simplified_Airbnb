<% if @message.errors.empty? %>
  $("#chat").html("<%= j render 'messages' %>");
<% else %>
  var errors = "";
  <% @message.errors.full_messages.each do |message| %>
    errors += $("<div/>").html("<%= message %>").text() + " \n";
  <% end %>
  toastr.error(errors)
<% end %>